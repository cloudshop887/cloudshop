{
  "name": "cloudshop-root",
  "version": "1.0.0",
  "description": "Root package for CloudShop application",
  "scripts": {
    "dev": "concurrently -n \"FLASK_API,SHOP_APP\" -c \"blue,green\" \"npm run server\" \"npm run client\"",
    "launch": "npm run dev",
    "server": "cd flask-backend && python app.py",
    "client": "cd frontend && npm run dev",
    "install-all": "npm install && cd flask-backend && pip install -r requirements.txt && cd ../frontend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "concurrently \"cd flask-backend && python app.py\" \"cd frontend && npm start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}